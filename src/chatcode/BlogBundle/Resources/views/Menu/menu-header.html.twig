<nav>
    <ul>
        {% for item in menu %}
            <li>
                <a href="{{ path(item.path) }}" title="{{ item.title }}">{{ item.title }}</a>
            </li>
        {% endfor %}
    </ul>
</nav>